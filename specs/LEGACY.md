# specs/ — 历史实验产物

这里存放的是 LDD A阶段（M1–M3）期间手填或AI生成的 spec.json 文件，用于验证 schema-driven 范式。

**当前状态：仅供参考，不再主动维护。**

现役工作流已切换至 webapp（`webapp/`）+ prop/gameplay 模板（`templates/html/`）。
新产出的文档存放在 `docs/`，不再产出 spec.json。

| 文件 | 用途 |
|------|------|
| `demo_lighting_req.spec.json` | M1 手填 demo |
| `lighting_req_underground_parking_horror.spec.json` | M2 端到端验证产物 |
| `lighting_req_gangster_mansion.spec.json` | M3.1 真实案例（黑帮大宅）|
| `demo_bubble_diagram.spec.json` | M3.2 手填 demo |
| `bubble_diagram_gangster_mansion.spec.json` | M3.2 真实案例（主动线）|
| `bubble_diagram_test_warehouse.spec.json` | M3 测试用 |
| `*_abandoned_temple.spec.json` | v0.1.0 发布用虚构案例 |
| `level_overview_test_warehouse.spec.json` | M3.9 测试用 |
